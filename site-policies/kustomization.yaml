apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
 - oadp-backup/oadp-dap-config.yaml

configMapGenerator:
- files:
    - oadp-backup/ibu-backup-configmap.yaml
  name: oadp-cm
  namespace: openshift-adp

generatorOptions:
  disableNameSuffixHash: true
